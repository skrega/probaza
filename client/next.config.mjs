const nextConfig = {
    experimental: {
        scrollRestoration: true
    },
    images: {
        remotePatterns: [{
            protocol: 'http',
            hostname: '89.223.122.112'
        }]
    },
}

export default nextConfig;
